spring:
  application:
    name: simpleEngine
  cloud:
    config:
      url: http://configserver:8888

eureka:
  instance:
    hostname: simpleprocess
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/

server:
  port: 9998
