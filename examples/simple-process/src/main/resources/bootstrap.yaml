spring:
  application:
    name: simpleEngine
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configServer
eureka:
  instance:
    hostname: simpleprocess
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
