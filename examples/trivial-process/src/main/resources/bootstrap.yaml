spring:
  application:
    name: trivialEngine
  cloud:
    config:
      uri: http://configserver:8888

eureka:
  instance:
    hostname: trivialprocess
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/

server:
  port: 9999
