eureka:
  client:
    healthcheck:
      enabled: true

endpoints:
  info:
    enabled: true

eventServiceBaseUrl: http://${DOCKER_IP:localhost}:8081

camunda.bpm:
  metrics.enabled: false
  history-level: auto
  database:
    schema-update: drop-create
    type: h2
