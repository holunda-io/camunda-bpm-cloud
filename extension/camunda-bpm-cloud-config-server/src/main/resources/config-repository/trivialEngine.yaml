camunda.bpm:
  metrics.enabled: false
  history-level: auto

  database:
    schema-update: drop-create
    type: h2

info:
  app:
    engine: camunda
    version: 7.4
    instance: trivialEngine

eureka:
  instance:
    metadata-map:
      engine: camunda

server:
  port: 8082

